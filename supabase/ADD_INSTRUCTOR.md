# Добавление инструкторов в базу данных

## Инструкторы для добавления:
- Игорь Нагорский (миграция: `003_add_igor_instructor.sql`)
- Станислав Зигадло (миграция: `004_add_stas_instructor.sql`)

## ⚠️ ВАЖНО: Сначала создайте таблицы!

Если вы получили ошибку `relation "bot_instructors" does not exist`, значит таблицы еще не созданы. 

**Сначала выполните миграцию для создания таблиц** (см. раздел ниже), а затем добавляйте инструктора.

---

## Шаг 0: Создание таблиц (если еще не созданы)

### Через Supabase Dashboard:

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Создайте новый запрос
5. Откройте файл `supabase/migrations/002_create_bot_tables.sql` и скопируйте **весь его содержимый**
6. Вставьте в SQL Editor
7. Нажмите **"Run"** (Запустить)
8. Дождитесь сообщения "Success"

После этого таблицы будут созданы, и вы сможете добавлять инструкторов.

---

## Способ 1: Через Supabase Dashboard (рекомендуется)

### Пошаговая инструкция:

1. **Откройте Supabase Dashboard**
   - Перейдите на [https://app.supabase.com](https://app.supabase.com)
   - Войдите в свой аккаунт

2. **Выберите проект**
   - В списке проектов найдите и выберите ваш проект автошколы

3. **Откройте SQL Editor**
   - В левом меню найдите раздел **"SQL Editor"** (иконка с символом `</>` или текст "SQL Editor")
   - Нажмите на него

4. **Создайте новый запрос**
   - Нажмите кнопку **"New query"** (Новый запрос) в верхней части экрана

5. **Добавьте Игоря Нагорского**
   - Скопируйте SQL запрос из файла `003_add_igor_instructor.sql` или используйте запрос ниже:

```sql
INSERT INTO bot_instructors (
  name, 
  style, 
  experience, 
  pass_rate, 
  photo_url, 
  specialty, 
  reviews_count, 
  rating,
  is_active
) VALUES (
  'Игорь Нагорский',
  'Основатель и руководитель автошколы "Viktorija", опытный инструктор по вождению',
  'Многолетний опыт',
  '95%',
  '/igor-ready.png',
  'Основатель автошколы, профессиональная подготовка водителей. Автомобиль: Toyota Corolla',
  0,
  5.0,
  true
);
```

6. **Добавьте Станислава Зигадло**
   - В том же SQL Editor создайте новый запрос или добавьте после первого
   - Скопируйте SQL запрос из файла `004_add_stas_instructor.sql` или используйте запрос ниже:

```sql
INSERT INTO bot_instructors (
  name, 
  style, 
  experience, 
  pass_rate, 
  photo_url, 
  specialty, 
  reviews_count, 
  rating,
  is_active
) VALUES (
  'Станислав Зигадло',
  'Инструктор по вождению, педагогический стаж 18 лет',
  '18 лет',
  '93%',
  '/stas-ready.png',
  'Профессиональная подготовка водителей. Автомобиль: Skoda Octavia',
  0,
  4.9,
  true
);
```

7. **Выполните запросы**
   - Нажмите кнопку **"Run"** (Запустить) или используйте сочетание клавиш `Ctrl+Enter` (Windows) / `Cmd+Enter` (Mac)
   - Дождитесь сообщения об успешном выполнении (обычно появляется зеленое уведомление "Success")

8. **Проверьте результат**
   - В том же SQL Editor выполните проверочный запрос:
   ```sql
   SELECT * FROM bot_instructors ORDER BY created_at DESC;
   ```
   - Вы должны увидеть обоих инструкторов в списке

## Способ 2: Через Table Editor (альтернативный способ)

Если SQL Editor кажется сложным, можно добавить через интерфейс:

1. В левом меню выберите **"Table Editor"**
2. Найдите таблицу **"bot_instructors"** в списке
3. Нажмите **"Insert row"** (Добавить строку)
4. Заполните поля:
   - `name`: Игорь Нагорский
   - `style`: Основатель и руководитель автошколы "Viktorija", опытный инструктор по вождению
   - `experience`: Многолетний опыт
   - `pass_rate`: 95%
   - `photo_url`: /igor-ready.png
   - `specialty`: Основатель автошколы, профессиональная подготовка водителей. Автомобиль: Toyota Corolla
   - `reviews_count`: 0
   - `rating`: 5.0
   - `is_active`: ✓ (включить)
5. Нажмите **"Save"** (Сохранить)

## Способ 3: Через миграцию (для разработчиков)

Если вы используете Supabase CLI:

```bash
cd viktorija-web
supabase db push
```

Или выполните миграцию вручную через psql:
```bash
psql -h your-db-host -U postgres -d postgres -f supabase/migrations/003_add_igor_instructor.sql
```

## Проверка

После добавления проверьте, что инструктор появился:

```sql
SELECT * FROM bot_instructors WHERE name = 'Игорь Нагорский';
```

Должна вернуться одна строка с данными инструктора.

## Примечания

- ✅ Фото инструктора находится в `/public/igor-ready.png` (файл уже есть в проекте)
- 🚗 Автомобиль: Toyota Corolla
- 🔄 Инструктор будет отображаться в боте после перезагрузки страницы `/bot`
- 📸 Если фото не отображается, проверьте, что файл `igor-ready.png` находится в папке `public/`

## Возможные проблемы

**Проблема**: Ошибка "relation bot_instructors does not exist"
- **Решение**: Сначала выполните миграцию `002_create_bot_tables.sql` для создания таблиц

**Проблема**: Ошибка прав доступа
- **Решение**: Убедитесь, что вы используете правильный проект и у вас есть права на запись

**Проблема**: Фото не отображается
- **Решение**: Проверьте путь к файлу - он должен быть `/igor-ready.png` (с начальным слешем)
