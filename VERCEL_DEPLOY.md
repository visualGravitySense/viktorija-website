# 🚀 Деплой бота на Vercel

## 📋 Обзор

Этот документ описывает процесс деплоя бота автошколы на Vercel, в то время как основной сайт остается на GitHub Pages.

## 🎯 Архитектура

- **Основной сайт**: GitHub Pages (viktorijaautokool.ee)
- **Бот**: Vercel (отдельный деплой)
- **Backend**: Vercel Serverless Functions (если нужен)
- **База данных**: Supabase

## ✅ Шаг 1: Подготовка проекта

### 1.1. Убедитесь, что проект готов к деплою

```bash
# Проверьте, что проект собирается
npm run build
```

### 1.2. Проверьте переменные окружения

Убедитесь, что в `.env.local` указаны правильные значения:
```env
VITE_SUPABASE_URL=https://ваш-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=ваш-anon-key
```

## ✅ Шаг 2: Установка Vercel CLI (опционально)

Для деплоя через командную строку:

```bash
npm i -g vercel
```

Или используйте веб-интерфейс Vercel.

## ✅ Шаг 3: Деплой через веб-интерфейс (рекомендуется)

### 3.1. Создайте аккаунт на Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Зарегистрируйтесь или войдите через GitHub

### 3.2. Импортируйте проект

1. Нажмите **Add New Project**
2. Выберите репозиторий `viktorija-web` из GitHub
3. Vercel автоматически определит настройки

### 3.3. Настройте проект

**Framework Preset**: Vite

**Build Command**: 
```
npm run build
```

**Output Directory**: 
```
dist
```

**Install Command**:
```
npm install
```

### 3.4. Добавьте переменные окружения

В разделе **Environment Variables** добавьте:

- `VITE_SUPABASE_URL` = ваш Project URL из Supabase
- `VITE_SUPABASE_ANON_KEY` = ваш anon key из Supabase
- `VITE_STRIPE_PUBLISHABLE_KEY` = ваш Stripe publishable key (если используется)

### 3.5. Настройте деплой

**Root Directory**: оставьте пустым (или укажите `.` если нужно)

**Production Branch**: `main` или `master`

### 3.6. Деплой

Нажмите **Deploy**. Vercel автоматически:
1. Установит зависимости
2. Соберет проект
3. Задеплоит на уникальный URL

## ✅ Шаг 4: Деплой через CLI

### 4.1. Войдите в Vercel

```bash
vercel login
```

### 4.2. Деплой

```bash
cd viktorija-web
vercel
```

Следуйте инструкциям:
- Link to existing project? **No** (первый раз) или **Yes** (если уже есть проект)
- What's your project's name? **viktorija-bot** (или другое имя)
- In which directory is your code located? **./**

### 4.3. Продакшн деплой

```bash
vercel --prod
```

## ✅ Шаг 5: Настройка кастомного домена (опционально)

Если хотите использовать кастомный домен для бота:

1. В Vercel Dashboard перейдите в **Settings** → **Domains**
2. Добавьте ваш домен (например, `bot.viktorijaautokool.ee`)
3. Следуйте инструкциям по настройке DNS

## ✅ Шаг 6: Настройка GitHub Pages для основного сайта

Основной сайт должен оставаться на GitHub Pages:

1. Убедитесь, что GitHub Actions workflow настроен (`.github/workflows/deploy.yml`)
2. Основной сайт будет деплоиться на GitHub Pages автоматически
3. Бот будет доступен по URL Vercel

## 🔧 Настройка для раздельного деплоя

Если вы хотите деплоить только бота на Vercel, а основной сайт на GitHub Pages:

### Вариант 1: Использовать один репозиторий с разными настройками

1. Создайте отдельный проект в Vercel для бота
2. Настройте **Root Directory** в Vercel на подпапку (если бот в отдельной папке)
3. Или используйте фильтры деплоя в Vercel

### Вариант 2: Создать отдельный репозиторий для бота

1. Создайте новый репозиторий для бота
2. Скопируйте только файлы бота
3. Деплойте отдельно

## 📝 Структура после деплоя

```
viktorijaautokool.ee (GitHub Pages)
├── / (главная страница)
├── /features
├── /about
└── ... (основной сайт)

bot.viktorijaautokool.ee или viktorija-bot.vercel.app (Vercel)
└── /bot (страница бота)
```

## 🔄 Автоматический деплой

После настройки, каждый раз когда вы:
- Пушите изменения в ветку `main` или `master`
- Vercel автоматически задеплоит бота

## 🐛 Решение проблем

### Проблема: "Build failed"

**Решение:**
1. Проверьте логи сборки в Vercel Dashboard
2. Убедитесь, что `npm run build` работает локально
3. Проверьте, что все зависимости установлены

### Проблема: "Environment variables not found"

**Решение:**
1. Убедитесь, что переменные окружения добавлены в Vercel
2. Проверьте, что имена переменных начинаются с `VITE_`
3. Перезапустите деплой после добавления переменных

### Проблема: "Supabase connection failed"

**Решение:**
1. Проверьте, что переменные окружения правильные
2. Убедитесь, что URL и ключи Supabase корректны
3. Проверьте настройки CORS в Supabase

### Проблема: "Route not found"

**Решение:**
1. Проверьте конфигурацию `vercel.json`
2. Убедитесь, что роуты настроены правильно
3. Проверьте, что `index.html` находится в `dist`

## 📚 Дополнительные ресурсы

- [Документация Vercel](https://vercel.com/docs)
- [Документация Vite](https://vitejs.dev)
- [Настройка Supabase](SUPABASE_SETUP.md)

## ✅ Чеклист деплоя

- [ ] Проект собирается локально (`npm run build`)
- [ ] Создан аккаунт на Vercel
- [ ] Проект импортирован в Vercel
- [ ] Настроены переменные окружения
- [ ] Первый деплой успешен
- [ ] Бот доступен по URL Vercel
- [ ] Основной сайт остается на GitHub Pages
- [ ] Настроен кастомный домен (если нужен)
- [ ] Автоматический деплой работает
