# Настройка деплоя в репозиторий visualGravitySense/viktorija-website

## ✅ Что уже сделано:

1. ✅ Создан GitHub Actions workflow (`.github/workflows/deploy.yml`)
2. ✅ Workflow настроен для автоматического деплоя при пуше в `master` или `main`

## 📋 Следующие шаги:

### 1. Инициализация Git репозитория (если еще не сделано)

```bash
# Инициализировать git
git init

# Добавить все файлы
git add .

# Создать первый коммит
git commit -m "Initial commit: Setup deployment"

# Добавить remote репозиторий
git remote add origin https://github.com/visualGravitySense/viktorija-website.git

# Переименовать ветку в master (если нужно)
git branch -M master

# Запушить в репозиторий
git push -u origin master
```

### 2. Включить GitHub Pages через GitHub Actions

1. Перейдите на GitHub: https://github.com/visualGravitySense/viktorija-website
2. Откройте **Settings** (Настройки) репозитория
3. В левом меню найдите раздел **Pages**
4. В разделе **Source** выберите **GitHub Actions** (вместо "Deploy from a branch")
5. Нажмите **Save**

### 3. Проверить деплой

После пуша в `master` или `main`:
1. Перейдите в раздел **Actions** на GitHub
2. Вы увидите запущенный workflow "Deploy to GitHub Pages"
3. Дождитесь завершения (обычно 2-3 минуты)
4. После успешного деплоя сайт будет доступен по адресу:
   - **GitHub Pages URL**: `https://visualgravitysense.github.io/viktorija-website/`
   - **Кастомный домен** (если настроен): `https://viktorijaautokool.ee`

## 🔄 Автоматический деплой

После настройки, каждый раз когда вы:
- Пушите изменения в ветку `main` или `master`
- Или вручную запускаете workflow через **Actions → Deploy to GitHub Pages → Run workflow**

Сайт будет автоматически собираться и деплоиться на GitHub Pages.

## 📝 Примечания

- Workflow автоматически запускается при пуше в `master` или `main`
- Можно также запустить вручную через **Actions** → **Deploy to GitHub Pages** → **Run workflow**
- Если используется кастомный домен `viktorijaautokool.ee`, убедитесь, что он настроен в настройках GitHub Pages
- Base path настроен как `/` для работы с кастомным доменом

## 🐛 Устранение проблем

Если деплой не работает:
1. Проверьте, что в **Settings → Pages → Source** выбрано **GitHub Actions**
2. Проверьте логи в разделе **Actions**
3. Убедитесь, что workflow файл находится в `.github/workflows/deploy.yml`
4. Убедитесь, что ветка называется `master` или `main`
5. Проверьте, что у репозитория есть права на запись в Pages (Settings → Pages → Actions)

