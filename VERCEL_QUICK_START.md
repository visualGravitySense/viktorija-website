# ⚡ Быстрый деплой бота на Vercel

## 🎯 Цель

Задеплоить бота на Vercel за 5 минут, оставив основной сайт на GitHub Pages.

## ✅ Пошаговая инструкция

### 1. Откройте Vercel

Перейдите на [vercel.com](https://vercel.com) и войдите через GitHub.

### 2. Создайте новый проект

1. Нажмите **"Add New Project"**
2. Выберите репозиторий `viktorija-web`
3. Нажмите **"Import"**

### 3. Настройте проект

Vercel автоматически определит настройки, но проверьте:

- **Framework Preset**: `Vite`
- **Root Directory**: `.` (пусто)
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Install Command**: `npm install`

### 4. Добавьте переменные окружения

В разделе **Environment Variables** добавьте:

| Имя | Значение |
|-----|----------|
| `VITE_SUPABASE_URL` | Ваш Project URL из Supabase |
| `VITE_SUPABASE_ANON_KEY` | Ваш anon key из Supabase |
| `VITE_STRIPE_PUBLISHABLE_KEY` | Ваш Stripe key (если используется) |

**Где взять значения:**
- Supabase: [app.supabase.com](https://app.supabase.com) → Settings → API
- Stripe: [dashboard.stripe.com](https://dashboard.stripe.com) → Developers → API keys

### 5. Деплой

Нажмите **"Deploy"** и подождите 2-3 минуты.

### 6. Готово!

После деплоя бот будет доступен по URL:
- `https://viktorija-web-xxx.vercel.app`
- Или кастомный домен, если настроен

## 🔄 Автоматический деплой

После первого деплоя, каждый push в `main`/`master` автоматически задеплоит обновления.

## 🌐 Настройка кастомного домена

1. В Vercel Dashboard → **Settings** → **Domains**
2. Нажмите **"Add Domain"**
3. Введите домен (например, `bot.viktorijaautokool.ee`)
4. Следуйте инструкциям по настройке DNS

## 📝 Что дальше?

- Основной сайт остается на GitHub Pages
- Бот работает на Vercel
- Оба используют одну базу данных Supabase

## 🐛 Проблемы?

### Деплой не работает
→ Проверьте логи в Vercel Dashboard → Deployments

### Бот не подключается к Supabase
→ Проверьте переменные окружения в Vercel Settings

### Ошибки сборки
→ Убедитесь, что `npm run build` работает локально

## 📚 Дополнительно

- Подробная инструкция: `VERCEL_DEPLOY.md`
- Настройка Supabase: `SUPABASE_SETUP.md`
