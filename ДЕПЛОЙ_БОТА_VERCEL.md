# 🚀 Деплой бота на Vercel - Итоговая инструкция

## 📋 Что нужно сделать

1. ✅ Задеплоить бота на Vercel
2. ✅ Оставить основной сайт на GitHub Pages
3. ✅ Настроить переменные окружения

## ⚡ Быстрый старт (5 минут)

### Шаг 1: Откройте Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub

### Шаг 2: Импортируйте проект

1. Нажмите **"Add New Project"**
2. Выберите репозиторий `viktorija-web`
3. Нажмите **"Import"**

### Шаг 3: Настройте проект

Vercel автоматически определит настройки. Проверьте:

- **Framework Preset**: `Vite` ✅
- **Build Command**: `npm run build` ✅
- **Output Directory**: `dist` ✅

### Шаг 4: Добавьте переменные окружения

В разделе **Environment Variables** добавьте:

```
VITE_SUPABASE_URL = https://ваш-project-id.supabase.co
VITE_SUPABASE_ANON_KEY = ваш-anon-key
VITE_STRIPE_PUBLISHABLE_KEY = ваш-stripe-key (если используется)
```

**Где взять:**
- Supabase: [app.supabase.com](https://app.supabase.com) → Settings → API
- Stripe: [dashboard.stripe.com](https://dashboard.stripe.com) → Developers → API keys

### Шаг 5: Деплой

Нажмите **"Deploy"** и подождите 2-3 минуты.

### Шаг 6: Готово!

Бот будет доступен по URL:
- `https://viktorija-web-xxx.vercel.app`
- Или кастомный домен, если настроен

## 🎯 Архитектура после деплоя

```
Основной сайт (GitHub Pages)
└── viktorijaautokool.ee
    ├── / (главная)
    ├── /features
    ├── /about
    └── ... (все страницы кроме /bot)

Бот (Vercel)
└── viktorija-bot.vercel.app или bot.viktorijaautokool.ee
    └── /bot (страница бота)
```

## 🔄 Автоматический деплой

После настройки:
- Push в `main`/`master` → автоматический деплой на Vercel
- Основной сайт продолжает деплоиться на GitHub Pages через GitHub Actions

## 🌐 Настройка кастомного домена (опционально)

Если хотите использовать кастомный домен для бота:

1. В Vercel Dashboard → **Settings** → **Domains**
2. Нажмите **"Add Domain"**
3. Введите домен (например, `bot.viktorijaautokool.ee`)
4. Настройте DNS записи по инструкции Vercel

## ✅ Чеклист

- [ ] Создан аккаунт на Vercel
- [ ] Проект импортирован в Vercel
- [ ] Добавлены переменные окружения
- [ ] Первый деплой успешен
- [ ] Бот доступен по URL Vercel
- [ ] Основной сайт остается на GitHub Pages
- [ ] Автоматический деплой работает

## 🐛 Решение проблем

### "Build failed"
1. Проверьте логи в Vercel Dashboard → Deployments
2. Убедитесь, что `npm run build` работает локально
3. Проверьте, что все зависимости в `package.json`

### "Environment variables not found"
1. Убедитесь, что переменные добавлены в Vercel Settings → Environment Variables
2. Проверьте, что имена начинаются с `VITE_`
3. Перезапустите деплой после добавления переменных

### "Supabase connection failed"
1. Проверьте правильность `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`
2. Убедитесь, что значения скопированы полностью (без пробелов)
3. Проверьте настройки CORS в Supabase

### "Route not found" или "404"
1. Проверьте конфигурацию `vercel.json`
2. Убедитесь, что роуты настроены правильно в `App.tsx`
3. Проверьте, что `index.html` находится в `dist` после сборки

## 📚 Дополнительные ресурсы

- **Быстрый старт**: `VERCEL_QUICK_START.md`
- **Подробная инструкция**: `VERCEL_DEPLOY.md` (англ.) или `VERCEL_ДЕПЛОЙ.md` (рус.)
- **Настройка Supabase**: `SUPABASE_SETUP.md`
- **Документация Vercel**: [vercel.com/docs](https://vercel.com/docs)

## 💡 Важные замечания

1. **Основной сайт остается на GitHub Pages** - не нужно ничего менять в настройках GitHub Actions
2. **Бот деплоится отдельно на Vercel** - использует тот же код, но отдельный деплой
3. **Оба проекта используют одну базу данных Supabase** - изменения видны везде
4. **Автоматический деплой работает** - каждый push автоматически обновляет бота на Vercel

## 🎉 Готово!

После выполнения всех шагов:
- ✅ Бот работает на Vercel
- ✅ Основной сайт работает на GitHub Pages
- ✅ Оба подключены к Supabase
- ✅ Автоматический деплой настроен
