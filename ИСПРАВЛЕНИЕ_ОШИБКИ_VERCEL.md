# 🔧 Исправление ошибки "ERR_NAME_NOT_RESOLVED" на Vercel

## 🐛 Проблема

Ошибка `ERR_NAME_NOT_RESOLVED` при попытке подключиться к Supabase означает, что переменные окружения не загружаются в Vercel.

## ✅ Решение (5 минут)

### Шаг 1: Проверьте переменные окружения в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект `viktorija-website`
3. Перейдите в **Settings** → **Environment Variables**

### Шаг 2: Добавьте переменные окружения

Убедитесь, что добавлены следующие переменные:

| Имя переменной | Значение | Environment |
|----------------|----------|-------------|
| `VITE_SUPABASE_URL` | `https://ваш-project-id.supabase.co` | Production, Preview, Development |
| `VITE_SUPABASE_ANON_KEY` | `ваш-anon-key` | Production, Preview, Development |

**ВАЖНО:**
- ✅ Имена переменных должны начинаться с `VITE_`
- ✅ Выберите все окружения (Production, Preview, Development)
- ✅ Значения должны быть без пробелов и кавычек

### Шаг 3: Получите правильные значения из Supabase

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект

**Для VITE_SUPABASE_URL:**
3. Перейдите в **Settings** → **General**
4. Найдите **Project ID** (например: `pkwrduyxqsqnbtgplzzj`)
5. Постройте URL: `https://[ваш-Project-ID].supabase.co`
   - Пример: `https://pkwrduyxqsqnbtgplzzj.supabase.co`
6. Вставьте этот URL в `VITE_SUPABASE_URL`

**Для VITE_SUPABASE_ANON_KEY:**
7. Перейдите в **Settings** → **API Keys**
8. Найдите секцию **"Publishable key"**
9. Скопируйте ключ, который начинается с `sb_publishable_...`
10. Если не видите Publishable key, перейдите на вкладку **"Legacy anon, service_role API keys"** и скопируйте **anon public** ключ
11. Вставьте этот ключ в `VITE_SUPABASE_ANON_KEY`

### Шаг 4: Пересоберите проект

После добавления/изменения переменных окружения:

1. В Vercel Dashboard перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **"..."** (три точки) → **"Redeploy"**
4. Или сделайте новый commit и push в репозиторий

**ВАЖНО:** Vercel нужно пересобрать проект после добавления переменных окружения!

## 🔍 Проверка

После пересборки:

1. Откройте ваш сайт на Vercel
2. Откройте консоль браузера (F12)
3. Проверьте, что нет ошибок `ERR_NAME_NOT_RESOLVED`
4. Проверьте, что бот загружает данные из Supabase

## 🐛 Если проблема осталась

### Вариант 1: Проверьте правильность значений

1. Убедитесь, что URL Supabase правильный (начинается с `https://`)
2. Убедитесь, что ключ скопирован полностью (без пробелов)
3. Проверьте, что нет лишних символов

### Вариант 2: Проверьте логи сборки

1. В Vercel Dashboard → **Deployments**
2. Откройте последний деплой
3. Проверьте логи сборки на наличие ошибок

### Вариант 3: Проверьте настройки CORS в Supabase

1. В Supabase Dashboard → **Settings** → **API**
2. Проверьте раздел **CORS**
3. Убедитесь, что домен Vercel добавлен в разрешенные источники
4. Или используйте `*` для разработки (не рекомендуется для продакшена)

### Вариант 4: Проверьте через консоль браузера

Откройте консоль браузера (F12) и выполните:

```javascript
console.log('VITE_SUPABASE_URL:', import.meta.env.VITE_SUPABASE_URL);
console.log('VITE_SUPABASE_ANON_KEY:', import.meta.env.VITE_SUPABASE_ANON_KEY ? 'установлен' : 'НЕ УСТАНОВЛЕН');
```

Если переменные `undefined` или пустые - они не загружены в Vercel.

## 📝 Чеклист исправления

- [ ] Открыт Vercel Dashboard
- [ ] Переменные окружения добавлены в Settings → Environment Variables
- [ ] Имена переменных начинаются с `VITE_`
- [ ] Значения скопированы из Supabase правильно
- [ ] Выбраны все окружения (Production, Preview, Development)
- [ ] Проект пересобран после добавления переменных
- [ ] Проверена консоль браузера - нет ошибок
- [ ] Бот загружает данные из Supabase

## 💡 Важные замечания

1. **Переменные окружения в Vercel** доступны только после пересборки проекта
2. **Имена переменных** должны начинаться с `VITE_` для Vite проектов
3. **Значения** должны быть без кавычек и пробелов
4. **CORS** должен быть настроен в Supabase для домена Vercel

## 🎯 Быстрое исправление

1. Vercel Dashboard → Settings → Environment Variables
2. Добавьте `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`
3. Deployments → Redeploy
4. Готово!
