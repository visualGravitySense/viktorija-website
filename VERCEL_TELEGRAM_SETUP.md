# Настройка Telegram бота для Vercel

## ✅ Да, бот будет работать на Vercel!

Бот настроен для работы через **Vercel Serverless Function**, что решает проблему CORS и обеспечивает безопасность.

## Как это работает

1. **Клиентский код** вызывает `/api/telegram-notify`
2. **Vercel Serverless Function** обрабатывает запрос на сервере
3. **Telegram API** получает уведомление (без CORS проблем)

## Настройка для Vercel

### Шаг 1: Получить Chat ID

1. Откройте Telegram
2. Найдите бота `@userinfobot`
3. Отправьте `/start`
4. Скопируйте ваш Chat ID (число, например: `123456789`)

### Шаг 2: Добавить переменную окружения в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Settings** → **Environment Variables**
4. Нажмите **Add New**
5. Заполните:
   - **Name:** `VITE_TELEGRAM_ADMIN_CHAT_ID`
   - **Value:** ваш Chat ID (например: `123456789`)
   - **Environments:** выберите все (Production, Preview, Development)
6. Нажмите **Save**

### Шаг 3: Пересобрать проект

1. В Vercel Dashboard перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **⋯** (три точки) → **Redeploy**
4. Или просто сделайте новый commit и push

### Шаг 4: Проверить работу

1. Откройте ваш сайт на Vercel
2. Зарегистрируйтесь через Google
3. Проверьте Telegram - должно прийти уведомление! 🎉

## Структура файлов

```
viktorija-web/
├── api/
│   └── telegram-notify.ts    # Vercel Serverless Function
├── src/
│   └── services/
│       └── telegramService.ts  # Клиентский сервис
└── vercel.json                # Конфигурация Vercel
```

## Как это работает технически

### В Development (локально):
- Может работать напрямую через Telegram API (если нет CORS)
- Или использовать Vercel API route (если запущен через Vercel CLI)

### В Production (Vercel):
- Всегда использует `/api/telegram-notify` (Vercel Serverless Function)
- Токен бота хранится на сервере (безопасно)
- Нет проблем с CORS

## Проверка работы

### Тест 1: Регистрация
1. Зарегистрируйтесь через Google на Vercel
2. Проверьте Telegram - уведомление о новом пользователе

### Тест 2: Запись на урок
1. Запишитесь на урок
2. Проверьте Telegram - уведомление о записи

### Тест 3: Запрос психологу
1. Отправьте сообщение психологу
2. Проверьте Telegram - уведомление о запросе

## Устранение проблем

### Уведомления не приходят

**Проверьте:**
1. ✅ `VITE_TELEGRAM_ADMIN_CHAT_ID` установлен в Vercel
2. ✅ Проект пересобран после добавления переменной
3. ✅ Откройте консоль браузера (F12) - нет ли ошибок
4. ✅ Проверьте логи Vercel Function:
   - Vercel Dashboard → Deployments → выберите деплой → Functions → `telegram-notify`

### Ошибка "Chat not found"
- Убедитесь, что вы отправили сообщение боту
- Проверьте, что Chat ID правильный

### Ошибка в логах Vercel Function
- Откройте Vercel Dashboard → Deployments → Functions
- Проверьте логи `telegram-notify`
- Убедитесь, что токен бота правильный

## Безопасность

✅ **Токен бота** хранится в Serverless Function (не в клиентском коде)  
✅ **Chat ID** передается через переменную окружения  
✅ **Нет проблем с CORS** - запросы идут через сервер

## Дополнительная информация

- Подробная инструкция: `TELEGRAM_BOT_SETUP.md`
- Быстрый старт: `БЫСТРЫЙ_ЗАПУСК_БОТА.md`
