# Компоненты для работы с контентом из Supabase

Этот каталог содержит примеры компонентов для работы с контентом, управляемым через Supabase.

## 📁 Файлы

- `ContentExample.tsx` - Примеры компонентов для работы с контентом

## 🚀 Использование

### FAQExample

Компонент для отображения FAQ из Supabase:

```tsx
import { FAQExample } from '../components/content/ContentExample';

function MyPage() {
  return <FAQExample />;
}
```

### BlogPostsExample

Компонент для отображения блог постов:

```tsx
import { BlogPostsExample } from '../components/content/ContentExample';

function BlogPage() {
  return <BlogPostsExample />;
}
```

### ContentEditorExample

Простой редактор для создания нового контента (требует аутентификации в Supabase):

```tsx
import { ContentEditorExample } from '../components/content/ContentExample';

function AdminPage() {
  return <ContentEditorExample />;
}
```

## 📝 Примечания

- Все компоненты автоматически используют текущий язык из i18next
- Компоненты показывают состояние загрузки и ошибки
- Для создания/обновления контента требуется аутентификация в Supabase

## 🔧 Кастомизация

Вы можете использовать эти компоненты как основу для создания своих собственных компонентов. См. также:
- `src/hooks/useContent.ts` - хуки для получения контента
- `src/hooks/useContentManagement.ts` - хуки для управления контентом
- `src/services/contentService.ts` - сервисы для работы с Supabase

