# 🚀 Деплой бота на Vercel

## 📋 Быстрый старт

### Шаг 1: Подготовка (2 минуты)

1. Убедитесь, что проект собирается:
   ```bash
   npm run build
   ```

2. Проверьте переменные окружения в `.env.local`:
   ```env
   VITE_SUPABASE_URL=https://ваш-project-id.supabase.co
   VITE_SUPABASE_ANON_KEY=ваш-anon-key
   ```

### Шаг 2: Деплой через веб-интерфейс (5 минут)

1. **Откройте [vercel.com](https://vercel.com)** и войдите через GitHub

2. **Нажмите "Add New Project"**

3. **Выберите репозиторий** `viktorija-web`

4. **Настройте проект:**
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`

5. **Добавьте переменные окружения:**
   - `VITE_SUPABASE_URL` = ваш Project URL
   - `VITE_SUPABASE_ANON_KEY` = ваш anon key
   - `VITE_STRIPE_PUBLISHABLE_KEY` = ваш Stripe key (если используется)

6. **Нажмите "Deploy"**

7. **Готово!** Бот будет доступен по URL вида: `viktorija-web-xxx.vercel.app`

### Шаг 3: Деплой через CLI (альтернатива)

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Деплой
cd viktorija-web
vercel

# Продакшн деплой
vercel --prod
```

## 🎯 Архитектура после деплоя

- **Основной сайт**: GitHub Pages → `viktorijaautokool.ee`
- **Бот**: Vercel → `viktorija-bot.vercel.app` или кастомный домен
- **База данных**: Supabase

## 🔄 Автоматический деплой

После настройки, каждый push в `main`/`master` автоматически задеплоит бота на Vercel.

## 🌐 Настройка кастомного домена (опционально)

1. В Vercel Dashboard → **Settings** → **Domains**
2. Добавьте домен (например, `bot.viktorijaautokool.ee`)
3. Настройте DNS записи по инструкции Vercel

## 🐛 Решение проблем

### "Build failed"
→ Проверьте логи в Vercel Dashboard, убедитесь что `npm run build` работает локально

### "Environment variables not found"
→ Убедитесь, что переменные добавлены в Vercel и начинаются с `VITE_`

### "Supabase connection failed"
→ Проверьте правильность переменных окружения в Vercel

## 📚 Подробная инструкция

См. `VERCEL_DEPLOY.md` для детальной информации.

## ✅ Чеклист

- [ ] Проект собирается локально
- [ ] Создан аккаунт на Vercel
- [ ] Проект импортирован
- [ ] Добавлены переменные окружения
- [ ] Деплой успешен
- [ ] Бот доступен по URL Vercel
- [ ] Основной сайт остается на GitHub Pages
